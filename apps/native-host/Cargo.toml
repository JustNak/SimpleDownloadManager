[package]
name = "simple-download-manager-native-host"
version = "0.3.3-alpha"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
url = "2.5"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"
